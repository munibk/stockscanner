# Zerodha credentials — never commit these
zerodha.cfg
.zerodha_token.json

# User's personal watchlist (local to each machine)
watchlist.json

# Cached daily NSE bhavcopy files (re-downloaded as needed)
.bhavcache/

# In-progress / resumable scan checkpoints (local recovery state)
.scancache/

# Python cache
__pycache__/
*.py[cod]
*.pyo
.pytest_cache/

# Virtual environments
venv/
.venv/
env/

# OS
.DS_Store
Thumbs.db
